# OrderSync - Frontend

## Setup

```bash
npm install
npm run dev
```

Frontend runs at `http://localhost:3000`.

Make sure the backend API is running at `http://localhost:8000`.

## Build

```bash
npm run build
npm run preview
```
